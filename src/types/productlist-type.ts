export interface productlistType {
  id: number | string;
  title: string;
  description: string;
  price: number;
  image: string;
}

export interface CartItem {
  id: number | string;
  title: string;
  description: string;
  price: number;
  image: string;
}
